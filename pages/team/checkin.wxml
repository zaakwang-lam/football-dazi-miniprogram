<!-- pages/team/checkin.wxml -->
<view class="page">
  <view class="hero">
    <view class="hero-icon">📍</view>
    <view class="hero-title">考勤打卡</view>
    <view class="hero-sub">{{today}}</view>
  </view>

  <view class="location-card">
    <view class="loc-label">当前位置</view>
    <view class="loc-name">{{location.name}}</view>
    <view class="loc-distance">距离场地 {{location.distance}} 米</view>
    <view class="btn-primary checkin-btn" bindtap="onCheckin">
      {{checked ? '✓ 已打卡' : '立即打卡'}}
    </view>
  </view>

  <view class="section-title">今日出勤 ({{attendList.length}}/{{members.length}})</view>

  <view class="member-list">
    <view class="member-item" wx:for="{{members}}" wx:key="id">
      <view class="member-avatar">{{item.shortName}}</view>
      <view class="member-info">
        <text class="member-name">{{item.name}}</text>
        <text class="member-status {{item.checked ? 'checked' : 'unchecked'}}">
          {{item.checked ? '✓ 已打卡' : '未打卡'}}
        </text>
      </view>
    </view>
  </view>
</view>